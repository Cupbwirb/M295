/*package com.ubs.projekt.m295projekt;

import ch.ubs.m295.generated.v1.dto.Tier;
import com.ubs.projekt.m295projekt.dto.TierDB;

public class DBConvert {
    public static Tiere convert(Tier tierDB) {
        Tiere tier = new Tiere();
        tier.tierId(tierDB.getTierId());
        tier.setTierName(tierDB.getTierName());
        tier.setTierAlter(tierDB.getTierAlter());
        tier.setArtId(tierDB.getArtId());
        return tier;
    }

    public static TierDB convert(Tier tier) {
        TierDB tierDB = new TierDB();
        tierDB.setTierId(tier.getTierId());
        tierDB.setTierName(tier.getTierName());
        tierDB.setTierAlter(tier.getTierAlter());
        tierDB.setArtId(tier.getArtId());
        return tierDB;
    }
}*/
